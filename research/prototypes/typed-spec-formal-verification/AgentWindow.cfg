\* TLC config for AgentWindow.tla. Run:
\*   java -jar tla2tools.jar AgentWindow.tla -config AgentWindow.cfg
\* TLC was NOT available in the authoring environment; the Node mini-checker is
\* the runnable evidence and finds the same counterexample.

SPECIFICATION Spec

CONSTANTS
    Agents = {"writer", "reader"}
    Tools  = {"Read", "Write", "Bash"}
    MaxDepth = 3

INVARIANT NoContractEscape

\* NoStaleWindow is an invariant ONLY if SubagentStop is guaranteed; with an
\* abandoned child (no Stop) it is a liveness defect, which the Node L1 check
\* witnesses directly. Listed here as the property the generated model targets.
\* INVARIANT NoStaleWindow
