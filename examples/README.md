# Examples

- **Testing & eval** → [`harness/`](harness/README.md) — test your Claude Code
  harness (hooks/skills/settings) across three tiers, plus the one real plugin
  walked through every tier. Quick look: **`npm run demo:plugin`**.
- **Typed specs (Pillar 1)** — [`SKILL.md.spec.ts`](SKILL.md.spec.ts),
  [`railway/`](railway/) — how a `.spec.ts` compiles to an instruction file. See
  [verifying instruction files](../docs/verifying-instruction-files.md).
