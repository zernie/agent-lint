# Benchmark run artifacts (regenerated by run.sh — not source)
.work/
results.csv
run.log

# Stray generate-types output inside task fixtures
tasks/**/.vigiles/
tasks/.vigiles/

# TDD / process benchmark artifacts
.work-tdd/
results-tdd.csv
run-tdd.log

# Context-load benchmark artifacts
.work-ctx/
results-ctx.csv
run-ctx.log
.corpus.txt

# refs benchmark artifacts
.work-refs/
results-refs.csv
run-refs.log
