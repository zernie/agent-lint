{
  "name": "agent-lint",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "agent-lint": "./cli.mjs"
  },
  "files": [
    "cli.mjs",
    "validate.mjs",
    "install-skills.mjs",
    "action.mjs",
    "action.yml",
    ".claude/"
  ],
  "scripts": {
    "test": "node --test validate.test.mjs install-skills.test.mjs",
    "fmt": "prettier --write .",
    "fmt:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  }
}
